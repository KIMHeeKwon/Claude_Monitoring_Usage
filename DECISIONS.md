# DECISIONS — Claude_Monitoring_Usage

> G2(인터뷰) 산출물. 폭발 반경이 큰 순서(P0 → P2)로 적는다. **미결 항목은 사용자가 답하기 전까지
> "추천안"을 전제로 진행하며, 답이 다르면 되돌린다.** 설계 본문은 [[ARCHITECTURE]], 조사는 [[PRIOR-ART-SURVEY]].

## 미결 P0 — 답이 아키텍처를 바꾸는 질문

### ~~Q1. "Claude 사용량"은 구독 한도(5시간 창·주간 %)를 뜻하는가?~~ → 확정 D6 (2026-09-03)

- 사용자 답: **맞다. 단, Pro 사용자도 감안할 것.** → [[ARCHITECTURE]] §2 A5, §4 "창(window) 부재 허용".

### ~~Q2. 동료들은 모두 Claude Code를 쓰는가?~~ → 확정 D7 (2026-09-03), 전제 하나는 유지

- 사용자 답: **Max와 Pro 둘 다 대상.** 요금제 두 가지를 모두 지원한다.
- 유지되는 전제: 한도 값은 Claude Code를 통해서만 얻을 수 있으므로(Q4 참조), **모든 사용자가 Claude Code에
  로그인되어 있다**는 전제 A2는 그대로다. 웹만 쓰는 사람은 시스템 지표만 본다.

### Q3. 프레임워크 — Tauri v2(추천) vs Electron

| 기준 | Tauri v2 (Rust 코어 + WebView) | Electron (Node + Chromium) |
|---|---|---|
| 설치 파일 크기 | 수 MB | 100MB 이상 |
| 상주 메모리 | 낮음 | 높음 (Chromium 동봉) — "상시 표시 앱"에 불리 |
| CPU·메모리·GPU 읽기 | `sysinfo`, `nvml-wrapper`, IOKit 직접 호출 가능 | `systeminformation` 패키지, GPU는 CLI 호출 의존 |
| 항상-위·트레이·투명창 | 지원 (v2) | 지원 |
| 개발 비용 | Rust 툴체인 설치 필요(이 PC에 없음). 코어 코드는 200~400줄 규모라 C 개발자에게 부담이 크지 않음 | Node는 이미 설치됨. JS만으로 됨 |
| 서명 없는 배포 | 동일 조건 | 동일 조건 |

- **추천안: Tauri v2.** 하루 종일 떠 있는 앱은 상주 비용이 곧 품질이다. 시스템 지표를 OS API로 직접 읽는 데도
  Rust 쪽이 깔끔하다. 사용자가 Rust 도입을 원하지 않으면 Electron으로 가되, [[ARCHITECTURE]] §3의 모듈 구조는
  그대로 쓴다.
- 조사 확인 (2026-09-03, [[PRIOR-ART-SURVEY]] §4·§5): 같은 조합(Tauri + sysinfo + nvml-wrapper)의 6~8MB 트레이
  모니터와 Windows용 Claude 사용량 트레이 앱(Win-CodexBar)이 이미 있다. Electron의 `systeminformation`은 Windows에서
  지표를 읽을 때마다 PowerShell을 띄운다. 추천 유지.
- 기각: Python(pystray+tk) — 배포 패키징과 창 품질이 나쁘다. Swift+C# 이중 네이티브 — 코드베이스 두 개는 R6 위반.
  웹 페이지만 — 항상-위 창과 로컬 토큰·시스템 지표 접근이 불가능하다.

### Q4. 한도 값을 어느 경로로 읽는가 — 공식 statusline만 vs 비공식 조회도 (옵트인)

- 사실 ([[PRIOR-ART-SURVEY]] §1·§2, 2026-09-03 조사):
  - **공식 경로가 있다.** Claude Code statusline JSON의 `rate_limits`가 `/usage`와 같은 %를 준다. 단 Claude Code
    세션이 떠 있을 때만 갱신된다.
  - **비공식 경로(`/api/oauth/usage` + 로그인 토큰)는 약관 문제가 있다.** Anthropic이 2026-02-19에 "구독 계정
    OAuth 토큰의 제3자 도구 사용은 소비자 약관 위반"으로 명문화했고, 조회와 추론을 구분하지 않았다. 실제 차단은
    추론 도구에만 내려졌고 조회 도구(CodexBar 등)는 2026-09-01까지 계속 배포 중이다. 계정 제재 사례는 조사에서
    발견되지 않았다 (부재의 증거는 아니다).
- **추천안: 공식 경로를 기본으로 하고, 비공식 경로는 기본 꺼짐의 옵트인으로 둔다.** 동료에게 나눠 주는 앱이므로
  각자가 고지를 읽고 결정하게 한다 ([[ARCHITECTURE]] §5.1). 어느 경로든 끊김은 정상 상태로 설계한다 (§6).
- 다른 선택지: (a) 공식 경로만 — 정책 위험 0, 대신 Claude Code를 닫아 두는 시간에는 값이 멈춘다(`stale`).
  (b) 비공식 경로만 — 항상 최신이지만 전원이 약관 위험을 진다. 추천안은 (a)에 (b)를 개인 선택으로 얹은 것이다.
- 사용자에게 묻는 것: **Claude Code를 닫아 둔 동안 값이 멈추는 것(a)을 받아들이는가, 아니면 옵트인(b)을 자기
  PC에서 켜겠는가.**

## 미결 P1

### Q5. 서명 없이 배포를 시작하는가?

- **추천안: 예.** 2~3명 시범 배포는 "우클릭 → 열기" 안내로 충분하다. 인원이 늘거나 회사 IT 정책에 걸리면
  서명을 도입한다 (Windows: Azure Trusted Signing, macOS: Developer ID + 공증).

### Q6. 토큰·비용 패널(로컬 기록 집계)을 1단계에 넣는가?

- **추천안: 아니오 (M5로).** R6("아주 간단")과 Q1의 답에 달렸다.

## 확정 (사용자 지시로 결정됨)

| ID | 결정 | 근거 | 일자 |
|---|---|---|---|
| D1 | 저장소는 `KIMHeeKwon/Claude_Monitoring_Usage` (개인 계정) | 사용자 제공 | 2026-09-03 |
| D2 | 표시 항목에 CPU·메모리·GPU 사용률 포함 | 사용자 요청 | 2026-09-03 |
| D3 | 화면 시안(G3)은 사용자와 별도 세션에서 함께 만든다. 초벌 스케치 `docs/mockups/ui-directions.html`은 확정본이 아님 | 사용자 지시 | 2026-09-03 |
| D4 | 토큰은 읽기 전용. 앱이 갱신(refresh)하지 않는다 | Claude Code 로그인과의 충돌 방지 | 2026-09-03 (설계 판단, 사용자 확인 전) |
| D5 | 서버 없음. 각자 자기 계정 | 사용자 요청("주변 사람들과 같이") + 단순성 | 2026-09-03 |
| D6 | "Claude 사용량" = 구독 한도 사용률(5시간 창·주간). Pro 사용자도 감안 | 사용자 답변 Q1 | 2026-09-03 |
| D7 | 대상 요금제는 Max와 Pro 둘 다 | 사용자 답변 Q2 | 2026-09-03 |
